
class configs:
    def __init__(self):
        self.aws_access_key = "<replace with you aws account's access key id>"
        self.aws_secret_key = "<replace with you aws account's secret access key>"
        self.jetpack_api_key = "<replace with base64 encoding of your jetpack username::password>"
        self.bucket_name = "<replace with your aws_bucket_name>"